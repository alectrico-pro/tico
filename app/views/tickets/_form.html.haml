= form_for @ticket, html: { class: 'form-horzontal' } do |f|
  .form-group
    = f.label 'asunto', class: 'label-control'
    = f.text_field :asunto, autofocus: true, class: 'form-control'
  .form-group
    = f.label 'estado', class: 'label-control'
    = f.select :status, Ticket.statuses.keys, class: 'form-control'
  .form-group
    = f.label 'contenido', class: 'label-control'
    = f.text_area :contenido, class: 'form-control'
  .form-group
    = f.submit class: 'btn btn-primary', :value => 'Guardar'
